import { buildClient } from '@datocms/cma-client';
// import { masterToken } from './getData';

// export async function catchPokemon(itemId, lista, pokemon){
//     const client = buildClient({ apiToken: masterToken });
//     const item = await client.items.update(itemId, {
//         pokemons: [...lista, pokemon]
//     });
//     console.log(client.items);
// }